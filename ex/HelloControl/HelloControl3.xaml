<UserControl
    x:Class="HelloControl.HelloControl3"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HelloControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="Control">
    
    <Grid>
        <Border x:Name="BG_Border" BorderBrush="{Binding Foreground, ElementName=Control}" BorderThickness="5" CornerRadius="5" Height="Auto" Width="Auto" Margin="10">
            <TextBlock x:Name="TB_Message" Foreground="{Binding Foreground, ElementName=Control}" FontSize="72" Text="{Binding Message, ElementName=Control}" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
